<Window x:Class="Timer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Timer" Height="450" Width="800"
		Background="#80000000" AllowsTransparency="True" WindowStyle="None" Topmost="True" KeyDown="OnKeyDown" WindowState="Maximized">
	<Window.ContextMenu>
		<ContextMenu>
			<MenuItem Header="Reset" Click="OnResetClick"/>
		</ContextMenu>
	</Window.ContextMenu>
    <Grid>
        <TextBlock x:Name="_textBox" Text="12:34" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Yellow" FontSize="512"/>
    </Grid>
</Window>
